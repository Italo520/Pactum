version: '3.8'

services:
  web:
    build: .
    command: gunicorn --bind 0.0.0.0:5000 Pactum.wsgi:application
    volumes:
      - .:/app
    ports:
      - "5000:5000"
    environment:
      - SECRET_KEY=mg4rs6v_s6r4_yg*+c(oy$*+4lljn!!svt4ryj*@bm!m3d5i$&
      - DEBUG=True
      - ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,*.replit.dev,*.replit.app,*.replit.co
      - DATABASE_URL=sqlite:///./db.sqlite3
